# PostCSS web

Example of using [PostCSS] in the browser.

## Quick start

```
git clone https://github.com/akarachen/postcss-web
cd postcss-web
pnpm install
pnpm build:playground
pnpm vite preview
```